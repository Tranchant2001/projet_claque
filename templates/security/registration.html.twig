{% extends 'base.html.twig' %}

{% block content %}
    <h1> Inscription sur le site </h1>
    {{ form_start(form) }}
{#    {% form_theme form 'form/bootstrap_3_layout.html.twig' %}#}
    {{ form_row(form.username) }}
    {{ form_row(form.email) }}
    {{ form_row(form.password,{'label':'Mot de passe','attr':{'placeholer':'Plus de 10 caractères'}}) }}
    {{ form_row(form.confirm_password,{'label':'Confirmez le précédent mot de passe'}) }}
    <button type="submit" class="btn btn-success"> Créer le compte ! </button>
    {{ form_end(form) }}
    
{% endblock %}
